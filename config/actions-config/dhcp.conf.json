{
    "README": [
        "Default config for Actions",
        "As all config fields are defaulted in schema, an action",
        "may have no config",
        "The following is just a sample"
    ],
    "anomalies-dhcp-relay:dhcp-relay-discard": {
        "timeout": 60,
        "heartbeat-interval": 5
    }
}

